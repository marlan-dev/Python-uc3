import time

num=int(input("Digite um número a ser escolhido pela tabuada: "))

inicio=time.time()

for i in range (1, 50):
    print(f"{num} x [i] = {num * i}")

final = time.time

print (f"\nTempo de execução: {final - inicio:.6f} segundos")
